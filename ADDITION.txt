Changes that i made after the meeting on 12th august.
Installed postgresql
configured with  the application. 
got neccessary dependancies
create the .env file and enter the credentials of the connection in it. 
create database/connection and in it made two functions, testconnection and syncdatabase. 
made sequelize instance in it to connect to the db. 
after the connection, create the model/employee.js. 
then write the updated controller api, load the employee model. 
    sync the model, 


TEST:
install 'npm install --save-dev jest supertest'
create the test cases
update the server.js 
chnages in the packages. 
make .env.test

add positve and negative cases. 

then add the authentication and the jwb token, create a auth and register controller. and then auth and register route files. 
after that get random access and refresh token like this
in terminal press 'node' 
then `require('crypto').randomBytes(64).toString('hex')`

then make auth files in controller and middle ware. 
middleware will have JWT verification function. 
middleware does this 

✅ Header exists
✅ Token extracted
✅ Token verified
✅ User info saved to req.user
✅ Continue to route handler

there was an issue with the cookie and then had to solve by logging the cookie request and the cookie it recieve. 
it showed that cookie was being requested 
but when looked closely it was sending the refresh token not the access token. 


To add the ZOD, 
We have to make a file called validate/employeeValidate, in it we have to create the employeeSchema, there we have two functions validateCreateEmployee and validateUpdateEmployee.
then after making these functions, we will use them in the routes like this:
.post(validateCreateEmployee, employeesController.createNewEmployee)
.put(validateUpdateEmployee, employeesController.updateEmployee)

// 26th august
Adding the validators in the employeesController
Session vs JWT
Session: User sends credentials, server sends the session id, user save the session id in the browser and sends with each request in cookies.
JWT: User submit the login form, server creates a JWT, browser puts the token in local storage and signs JWT header validation on future request.

REDIS
const redis = require('redis')
const RedisClient = redis.createClient(); 
RedisClient.setex('photos')
const DEFAULT_EXPIRATION = 3600; // 1 hour
redisclient.get('photos', (error))

in the server.js async fucntion, write this 
redisclient.setex("photos", DEFAULT_EXPIRATION, JSON.stringify(data)) => {
    if(!"photos"){
        return res.json(JSON.parse(photos))
    }
    else {
        do the previouus stuff
    }
}

get the photos from specific album id. 
const albumid = req.query.albumid
    photos?albumId = {albumid}


function getorsetcache(key, cb) {
    return new promise(resolve, reject)=>{
        redisClient.get(key, (error, data) =>
        if (error) return reject(errror)
        if(data != null) return resolved
        )

    }
}